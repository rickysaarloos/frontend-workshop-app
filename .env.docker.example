# Kopieer dit bestand naar .env voor gebruik met `docker compose up` (compose.yaml).
# Wijst naar de lokale studiedag-app nginx-container (poort 9000).
# LET OP: zonder /api-suffix — de frontend voegt /api zelf toe aan de fetch-calls.
VITE_API_URL=http://localhost:9000
